여러 영수증을 수집하면서 영수증의 종류는 2가지가 있었습니다.
- 세부 품목명이 존재하지 않는 영수증( 가맹점명, 결제위치, 결제일자, 총 결제금액 존재)
- 세부 품목명이 존재하는 영수증

이 2가지를 기준으로 영수증 기반 카테고리 분류 로직을 개발해 보았습니다.

세부 품목명이 존재하지 않는 영수증일 경우(classify_from_merchant)
- 1단계
1. 결제 위치를 기반으로 소상공인 DB 에서 주소 기반 가맹점명들 필터링
2. 필터링 된 가맹점명을 기반으로한 postgreSQL pg_trgm 텍스트 유사도 매칭 실행
3. 임계 값 기준으로 제일 높은 값을 기준으로 상권업종중분류명 추출(표준산업분류명에서 변경함) -> 임계값을 넘기지 못할경우 다음 단계 진행
- 2단계
1. 가맹점명을 기반으로 키워드 매칭 -> 사전 키워드 활용 -> 없을 경우 다음 단계 진행
- 3, 4단계
1. 중분류명을 받았을 경우 사전 중분류명 <-> 메인 카테고리 매칭 키워드로 바로 매핑 -> 못 받았을 경우 다음 단계 진입
- 5단계
1. SBERT 검색 (메인 카테고리 -> 세부 카테고리 순으로 전체 유사도 검색) 임계값 기준으로 최대 값을 산출

최종적으로 임계값 보다 낮은 값들만 나올경우 None 반환 -> 3단계 카테고리 분류 Small Language Model(gemini-2.5-flash-lite 연동 예정)

세부 품목명이 존재할 경우(classify_from_item)
- 1단계
1. 사전 키워드로 바로 매핑 검사 -> 실패시 다음단계
- 2단계
1. 세부카테고리에 대한 SBERT 유사도 검색 실시 -> 임계 값을 넘어가는 값중 최대 유사도에 대한 카테고리 선별 
2. 임계 값이 다 넘어가지 못하는 경우 None 반환 -> 3단계 카테고리 분류

개발을 하면서 여러 시행착오를 겪었습니다. 아마 SLM 쪽 만지면서 SBERT를 더 정교화 하지 않을까 싶습니다.
