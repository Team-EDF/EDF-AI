 [설계 변경 사항]

가맹점 분류 기존 : 가맹점명 또는 세부품목을 받으면 소상공인과 먼저 매칭 -> 매칭 실패시 SBERT 유사도 매칭-> GPT-mini-4o 분류 fallback

 가맹점 분류 최신

가맹점명만 존재(명세서): 소상공인과 완전매칭 시도 성공시 표준산업분류명 추출 -> 표준산업분류명 기준으로 메인 카테고리, 세부 카테고리와 임베딩 유사도 검색 실시(SBERT 개발 단계 시작예정) -> 매칭된 메인 카테고리의 탄소배출량과 금액을 곱해 탄소배출량 계산, 만일 완전 매칭에 실패할 경우 가맹점명을 기반으로 메인 카테고리와 임베딩 유사도 검색 실시, 이것도 안될경우 -> GPT -> 기타 분류

세부 항목을 받을 경우(영수증): 소상공인 건너뜀 바로 세부 카테고리와 임베딩 유사도 실시 -> 매칭될 경우 매칭된 세부 카테고리의 탄소배출량을 추출하여 탄소 배출량 계산 -> 실패할 경우 GPT -> 가맹점 기준으로 명세서 과정 재시도 -> 기타분류

탄소 배출량 이동 로직

-> 메인카테고리 별로 세부 카테고리가 나눠져 있고 세부 카테고리에서 계산한 탄소 배출량을 각 연결되어 있는 메인 카테고리에 누적합산 -> 추후 파이차트 및 그래프로 시각화
